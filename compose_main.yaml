services:
  nawah_app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nawah_monolith
    networks:
      - nawah
    ports:
      - "8080:80" # Frontend
      - "3000:3000" # Backend API
    restart: unless-stopped

networks:
  nawah:
    external: true
